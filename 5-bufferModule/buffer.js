// Creating Buffer

// Uninitiated Buffer
const buffer = Buffer.alloc(10);

// Array Buffer
const bufferArray = Buffer.from([1, 2, 3, 4, 5])

// String Buffer
const bufferString = Buffer.from('Hello World', 'utf-8');
